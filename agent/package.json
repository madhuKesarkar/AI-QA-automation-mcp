{
  "name": "bw-qa-loop",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous Linear-ticket -> Gherkin -> execution -> report loop for QA automation.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "capture-session": "npm run build && node dist/captureSession.js"
  },
  "dependencies": {
    "@anthropic-ai/bedrock-sdk": "^0.32.0",
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
